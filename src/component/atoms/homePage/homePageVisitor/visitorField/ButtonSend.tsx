import React from "react";
import {
  Box,
  Button,
  TextField,
  ThemeProvider,
  Typography,
  createTheme,
} from "@mui/material";

function ButtonSend() {
  return (
    <div>
      <Button variant="contained" color="primary">
        Try for free
      </Button>
    </div>
  );
}

export default ButtonSend;
